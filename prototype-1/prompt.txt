=== USER PROMPT 1: THE INITIAL IDEA ===
Hey! I want to build a quick terminal-based script for an emergency response app called LifeLine-AI. I don't need a visual user interface right now, just a clean console output that handles a countdown.

Here is the flow:
1. Print an emergency trigger message (e.g., patient reporting severe chest pain).
2. Print that it's locating ambulances near coordinates (19.2833, 72.8711).
3. Send a "Loud Ping" broadcast to 3 units in a 5km radius.
4. Start a strict 30-second countdown timer that logs the remaining time every 5 seconds (30s... 25s... 20s...).
5. If the timer hits 0, print a fallback message saying it's automatically contacting public emergency services instead.

Can you write a simple script for this?

---

=== AI ASSISTANT REPLY 1 ===
[The AI generated a basic script that initialized the variables, ran a standard setInterval loop for 30 seconds, and printed the timeout message once the countdown reached zero.]

---

=== USER FOLLOW-UP PROMPT 2: MAKING IT DYNAMIC ===
The countdown loop works, but right now it always runs out and calls public services. I want this simulation to behave like a real network where things happen dynamically. 

Can you update the script so that it simulates a random acceptance rate from the ambulances while the timer is ticking? 

If an ambulance randomly accepts the trip before the 30 seconds are up (like at the 14-second mark), it should instantly clear the timer loop, stop the countdown, and print:
"[SUCCESS] City Private Med (AMB-01) has ACCEPTED the trip!"
"[FINISH] Hospital API Handshake Complete. Route Cleared."

If the randomizer doesn't trigger a success within those 30 seconds, only then should it hit the fallback and call public services.

---

=== AI ASSISTANT REPLY 2 (FINAL CODE GENERATION) ===
[The AI updated the script, incorporating a conditional random check inside the timing loop to handle both the automated random success path and the 30-second public services timeout fallback entirely on its own.]
